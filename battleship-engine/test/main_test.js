const expect = require("chai").expect;

// Test suite
describe("Mocha is working", () => {
  // Test spec (unit test)
  it("should run our tests using npm", () => {
    // ok is an insertion method.
    expect(true).to.be.ok;
  });
});
